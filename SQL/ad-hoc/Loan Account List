SELECT COUNT(*)
FROM RPT.T_PROJECT AS P
         INNER JOIN
     RPT.T_CONTRACT AS C
     ON P.PRIMARY_CONTRACT_ID = C.CONTRACT_ID
WHERE P.PTO_AWARDED IS NULL
  AND P.INSTALLATION_COMPLETE <= DATEADD('D', -90, CURRENT_DATE)
  AND C.RECORD_TYPE = 'Solar Loan'