WITH PROCESS_START AS (
    SELECT F.*
         , TO_DATE(DATEADD(d, F.WEEK_BATCH * 7, '2013-11-06')) AS BATCH_DATE
    FROM D_POST_INSTALL.V_FILINGS_BATCH_ACCOUNT_LIST AS F
)

   , PROCESS_END AS (
    SELECT NULL
)

, MAIN AS (
    SELECT NULL
    FROM PROCESS_START AS S
    LEFT JOIN PROCESS_END AS E
)

SELECT *
FROM PROCESS_START
LIMIT 5